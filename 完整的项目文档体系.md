建议规划并要求 Trae 交付以下完整的项目文档体系：

一、 核心项目入口文档
1. README.md（项目主入口与快速上手）
项目定位：简述本地化、隐私安全、零幻觉、支持文档溯源的轻量 RAG 工具。
核心特性清单：
本地文件夹即插即用解析（Markdown/PDF/TXT/DOCX）。
纯本地 Ollama 大模型与 Embedding 支撑（100% 离线，无数据外泄）。
零幻觉兜底机制（无依据直接判定“未找到”）。
溯源引用展示（文件名 + 页码/段落 + 定位锚点）。
极简快速启动（Quick Start）：
3 步启动示例（依赖安装、Ollama 模型拉取、运行启动命令）。
目录结构说明：清晰列出项目各模块与配置目录的作用。
二、 用户操作与排障手册
2. USER_GUIDE.md（用户使用手册）
知识库装载与管理：
如何指定本地待检索文件夹路径。
支持的文件格式及文档排版最佳实践（提升分块与匹配精度）。
增量文件更新与重新向量化构建（Re-index）操作。
交互检索与对话模式：
命令行模式（CLI）或极简 Web/Chat 界面的交互使用方式。
参数调节说明（如检索条数 Top-K、相似度阈值 Score Threshold 等）。
溯源结果解读：
如何根据输出的 [出处: 文档名#页码] 快速比对本地原文。
3. TROUBLESHOOTING.md（故障排查与 FAQ 手册）
Ollama 本地服务异常：服务未启动、端口占用（11434）、显存溢出（OOM）或 CPU 占用过高的应对策略。
检索无结果排查：为什么已有文件却提示“知识库中未找到相关信息”（排查分块重叠度、向量模型匹配度或阈值过严格问题）。
文档解析报错：损坏文件、加密 PDF、编码异常（GBK vs UTF-8）的排查与修复流程。
三、 环境部署与运维文档
4. DEPLOYMENT_GUIDE.md（本地部署与模型配置指南）
前置依赖要求：
Python 版本规范（如 3.10+）。
Ollama 安装与验证步骤。
推荐本地模型组合（例如：Embedding 使用 bge-large-zh / nomic-embed-text，LLM 使用 qwen2.5 / llama3 等）。
本地运行环境配置：
虚拟环境构建（venv 或 conda）。
离线环境/内网环境的部署适配说明。
持久化与清理：
本地向量数据库存储路径（如 Chroma/FAISS/SQLite 本地目录）及缓存清理机制。
四、 技术架构与规范文档
5. ARCHITECTURE.md（系统架构与设计说明书）
三阶段完整流程图：
知识库搭建：文档加载（Loader） → 结构化切分（Chunking） → 向量化（Embedding） → 向量库入库（Vector Store）。
检索阶段：用户 Query 向量化 → 语义相似度召回（Similarity Search） → 阈值过滤（Score Filtering）。
生成阶段：组装上下文（Context） → 注入防幻觉系统提示词（System Prompt） → Ollama 推理 → 拼接出处元数据输出。
模块解耦设计：文档解析器、向量化接口、LLM 推理客户端的解耦规范。
6. CLI_API_REFERENCE.md（命令行参数与接口参考）
CLI 参数全量表：
--data-dir：指定知识库文件夹路径。
--llm-model / --embed-model：指定 Ollama 模型名称。
--threshold：控制回答触发的最低置信度阈值。
--strict：强制执行零幻觉兜底策略开关。
输入/输出 JSON 格式标准（如提供 API 模式时）。
五、 质量保证与零幻觉验证报告
7. VERIFICATION_AND_EVAL.md（零幻觉与溯源审计手册）
测试用例规范：
正向测试：文档包含明确事实，验证生成的准确性及引用出处精确度（文档名/页码）。
反向边界测试：提问知识库不存在的内容，验证系统是否100% 触发“知识库中未找到相关信息”，坚决不发生外推编造。
基准测试记录：记录在典型硬件配置下，知识库构建耗时与单次检索响应延迟。
📋 交付物清单一览（供 Trae 任务看板追踪）
序号
文档名称
定位与目标受众
1
README.md
项目总览与 3 分钟快速启动（开发者/使用者）
2
USER_GUIDE.md
知识库管理与日常检索交互指南（最终用户）
3
DEPLOYMENT_GUIDE.md
离线/本地环境依赖与 Ollama 模型配置（运维/管理员）
4
TROUBLESHOOTING.md
常见错误码、性能调优与 FAQ（最终用户/运维）
5
ARCHITECTURE.md
三阶段数据流与系统架构设计（二次开发者）
6
CLI_API_REFERENCE.md
命令行参数与接口全集说明（开发者）
7
VERIFICATION_AND_EVAL.md
零幻觉测试标准与溯源正确性验证矩阵（QA/审计）

---

## 实际交付映射（2026-09-03 更新）

上方清单是早期规划的「理想文档名」。实际项目已落地为两套文档，对应关系如下：

### A. 根目录文档（规划/规范层）

| 规划项 | 实际文档 |
|---|---|
| README.md | `Mini-RAG 知识检索系统.md`（项目主入口与快速上手） |
| 架构规划 | `本地轻量化 RAG 知识检索系统架构设计与工程实施规划书.md` |
| — | `DOC-01 知识库数据治理与预处理规范.md` |
| VERIFICATION_AND_EVAL.md | `DOC-02 零幻觉评估基准与测试用例集.md` |
| — | `DOC-03 Prompt 工程与护栏约束规范.md` |
| DEPLOYMENT_GUIDE.md | `DOC-04 硬件配置、模型选型与基线容量规划.md` |
| — | `DOC-05 端到端本地安全与隐私合规说明.md` |
| USER_GUIDE.md | `DOC-06 用户交互指南与溯源使用手册.md` |

### B. `_build/` 目录文档（实现层，与 `mini_rag/` 代码逐字对齐）

| 规划项 | 实际文档 | 说明 |
|---|---|---|
| 入口导航 | `00-START-HERE.md` | 各文档职责一览 |
| ARCHITECTURE.md + CLI_API_REFERENCE.md | `SPEC.md` | 技术真源，§1~§5 已逐节重写对齐代码 |
| **USER_GUIDE.md** | **`USAGE.md`** | **实际使用手册：添加文件/文件夹、索引、查询检索、维护、FAQ（本次新增）** |
| — | `DECISION.md` | 关键技术决策记录 |
| — | `DEV-PLAN.md` | 工程实施计划 |
| VERIFICATION_AND_EVAL.md | `EVAL.md` | 零幻觉评估与测试用例 |

> 维护约定：改动代码后，同步更新 `_build/SPEC.md`（技术）与 `_build/USAGE.md`（操作）；新增/替换语料目录后，重跑 `DENSE_MIN` 标定并更新 `SPEC.md` §5.4。