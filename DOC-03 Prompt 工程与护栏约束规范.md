DOC-03: Prompt 工程与护栏约束规范
文件编号: PROMPT-ENG-003
版本: v2.0.0（与实现同步 2026-09-04，此前 v1.1.0 为立项规划态）
适用范围: Ollama LLM 系统级指令锁定、推理护栏机制、生成后校验
对应实现: mini_rag/core/generator.py · hyde.py

1. LLM 推理层硬性参数护栏 (Inference Parameter Locking)

调用本地 Ollama `/api/chat` 时强制注入以下参数（generator.py::_chat）：

```json
{
  "model": "qwen3.5:4b",
  "stream": false,
  "keep_alive": "0",
  "think": false,
  "options": {
    "num_gpu": 0,
    "temperature": 0.0,
    "top_k": 1,
    "top_p": 0.1,
    "repeat_penalty": 1.15,
    "seed": 42,
    "num_ctx": 4096,
    "stop": ["<|im_end|>", "【官方检索上下文结束】", "用户提问："]
  }
}
```

三个**非显而易见但必须保留**的参数（删掉会直接导致线上故障）：

| 参数 | 值 | 为什么必须 |
|---|---|---|
| `num_gpu` | `0` | 强制纯 CPU。Ollama 默认自动走 Vulkan 调用显卡，本机 RX 470 会报 `failed to allocate Vulkan0 buffer` 崩溃 |
| `think` | `false` | qwen3.5 是 thinking 模型。不关会输出空串或极慢（实测慢 15~25 倍）。代码另有 `<think>` 残留正则作为第二道兜底 |
| `keep_alive` | `"0"` | 生成后立即卸载 3.4GB 模型。与 2.5GB embedding 模型同时驻留会打爆本机内存 |

v1.1.0 未包含 `think` 与 `num_gpu`，是当时模型/环境未暴露该问题。

2. 系统提示词模板 (System Guardrail Template)

以下是**代码中实际使用的** SYSTEM_PROMPT（generator.py:15-27），已按实测调优：
相比 v1.1.0，删掉了「让 LLM 自己输出溯源行」的要求 —— 引用改为系统程序化生成，
否则 LLM 编造引用的概率显著上升。

```
你是一个严谨的本地技术知识检索助手。你的唯一职责是依据下方【官方检索上下文】
回答用户的命令行与技术咨询。

【零幻觉铁律】
1. 你的回答必须 100% 严格来源于【官方检索上下文】。严禁使用你预训练获得的
   任何外部知识进行推测、补充、延伸或编造。
2. 若上下文为空、与问题无关，或未包含回答问题所需的完整信息，你必须且只能输出：
   知识库中未找到相关信息
   严禁追加任何道歉、解释或建议。
3. 命令行零容错：输出的每条命令、参数（flags）、路径必须与上下文逐字一致。
   严禁拼接或混用不同产品的工具。
4. 若上下文包含 WARNING / CAUTION / 高风险警示，必须在对应命令前单独一行
   加粗输出：**【高风险警示】**。

【输出格式】
- 先给结论，再给命令（命令用独立 markdown 代码块包裹）。
- 不要自行输出任何来源或溯源行，引用来源由系统统一生成。
```

上下文组装（`build_context`）：每个片段带 `[片段N | 来源: 文件名 | 第X页 | 章节: ...]` 头，
累计 token 超过 `MAX_CONTEXT_TOKENS=2800` 即停止拼装。空检索时注入 `（空）` 占位。

2.1 ⚠️ 规则 2 的严格性已拍板维持（P02 决策，2026-09-04）

规则 2 中「未包含回答问题所需的**完整信息**」是**有意为之的严格**，不是缺陷。

实测 P02（PowerStore 怎么更换 BBU？）：检索 hit@1 正确命中 BBU 文档，
但召回内容只有告警与识别说明、没有完整更换步骤，LLM 据此拒答。

这是**零幻觉严格性 vs 可用性**的显式权衡。放宽为「给确定部分信息 + 标注缺失」
或调大 `FINAL_TOP_N` 都会引入幻觉风险。**决策：维持严格，接受可用性代价。**

对照组 P01（svc_factory_reset）完整作答（结论 + 参数 + 命令块 + 4 条引用），
证明链路健康 —— 拒答是上下文驱动的，不是模型故障。

3. 前置拦截 (Pre-LLM Interceptor)

送入 LLM 之前有两层程序化拦截，均不消耗 LLM 算力：

```
查询 → 【证据闸】查询级语料证据判定 → 无证据则直接拒答（零模型，17~38ms）
     → 多路检索 + RRF 融合 + MMR
     → 【DENSE_MIN 硬阈值】过滤低于 0.60 的候选
     → 无候选则短路返回固定拒答串，不调 LLM
```

- **证据闸（查询级）**：规则 G——查询含 DF=0 的英文 token（语料零出现）→ 该专名 KB
  不覆盖 → 拒；规则 F——中文内容词全部 DF=0 且不在中英词典 → 拒。详见 README 第 4 节。
- **DENSE_MIN = 0.60**：换真实语料后重标定值。正例 top1 ∈ [0.706, 0.902]，
  负例 top1 ∈ [0.313, 0.436]，**无重叠**。v1.1.0 写的 0.65 是未标定的占位值。

4. L3 生成后校验（第三道防线，纯规则零模型）

`validate_answer()` 四条规则，全部只抓强信号，避免误伤忠实转述：

| # | 规则 | 判定 |
|---|---|---|
| 1 | 引用编号越界 | 答案中 `[N]` 的 N 超过片段数 → 编造引用 |
| 2 | 推断话术 | 命中 `INFERENCE_PHRASES`（据我所知 / 一般来说 / 理论上 …）→ 诉诸外部知识 |
| 3 | 命令/标识符逐字比对 | 含 `_ - . /` 的 token 必须出现在某块上下文中 |
| 4 | 版本号比对 | 三段 + 四段版本号必须出现在上下文中 |

**校验失败的处理是「降级为原文摘录」，不是拒答。** 纯原文同样零幻觉，
但可用性远高于拒答，这是刻意设计的第三态。

4.1 两个已修复的误伤 bug（2026-09-04，修复前 L3 几乎误杀所有英文答案）

| Bug | 现象 | 修复 |
|---|---|---|
| 句末句号被吃进 token | `now.` / `svc_factory_reset.` 被当作标识符比对，必然失败 | 匹配后 `rstrip(".")` + 英文缩写白名单 `_ABBREV_OK`（e.g / i.e / etc …） |
| ctx 只含 content | LLM 正确引用文件名（pwrstr-4-3-0-0-rn.pdf）被判编造 | ctx 补入 `file_name` |
| 版本号正则只认三段 | PowerStore 是四段版本号（4.3.0.0），等于完全漏检 | 正则补四段；两段不做（与章节号 1.2 难区分，误伤率高于收益） |

> 教训：L3 是规则系统，规则一错就是**系统性误杀**，且症状表现为「答案全变摘录」而非报错。
> 评估零幻觉不能只测检索层，必须端到端看生成结果。

5. HyDE Prompt（可选层）

`hyde.py` 生成 30-40 词假设性答案段落参与 dense 召回，救短查询/口语化查询。
`num_predict=80` + `HYDE_MAX_WORDS=45` 硬截断（纯生成 9.5s → 5.3s），
同样强制 `think:false` 与 `num_gpu:0`。
LRU 缓存落盘 `data/hyde_cache.jsonl`，key 归一化（去空白 + 小写 + 去首尾标点），
命中 0ms，跨进程持久化 —— CLI 每次都是新进程，不落盘的缓存无效。
