# ---- 核心依赖（必装）----
chromadb>=1.5
pymupdf>=1.24
pypdf>=4.0
python-docx>=1.1
beautifulsoup4>=4.12
lxml>=5.0
charset-normalizer>=3.3
jieba>=0.42

# 切分 token 计数（cl100k_base）。装不上会自动降级为字符启发式，功能不中断
tiktoken>=0.7

# 扫描页 OCR（约 50MB，纯 CPU）。不处理扫描件可以不装，未装时扫描页自动跳过
rapidocr-onnxruntime>=1.2

# ---- 可选：切换 Embedding / 向量库时才需要 ----
# openai>=1.0                  # EMBED_PROVIDER="openai"
# sentence-transformers>=2.2   # EMBED_PROVIDER="bge"（会拉 torch，约 2GB）
# qdrant-client>=1.7           # QdrantStore（需另起 Qdrant 服务）
# pymilvus>=2.3                # MilvusStore（需另起 Milvus 服务）
# llama-parse                  # OCR_PROVIDER="llamaparse"（云端 OCR，需 API key 且内容出网）
