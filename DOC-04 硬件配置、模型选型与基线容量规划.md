DOC-04: 硬件配置、模型选型与基线容量规划
文件编号: INFRA-SIZING-004
版本: v1.1.0
适用范围: 本地部署评估、模型适配与资源容量基线管理

1. 本地模型选型与评测基准 (Model Selection Matrix)
为实现轻量化且完全离线的 RAG 检索，Embedding 模型与生成 LLM 均需在保证足够精度的前提下，对本地算力友好。

1.1 Embedding 向量模型选型
模型名称
向量维度
最大序列长度
显存/内存占用
适用场景与推荐度
bge-small-zh-v1.5
512
512 Tokens
~350 MB
首选推荐：专为中文优化，体积小，CPU 上推理极快（<30ms）。
nomic-embed-text
768
2048 Tokens
~550 MB
备选推荐：支持超长上下文切片，中英文多语言混合文档友好。
bge-m3
1024
8192 Tokens
~2.2 GB
高配选项：支持密集/稀疏多路召回，但对低配硬件开销略大。


1.2 生成 LLM（基于 Ollama 引擎）选型
模型标识 (OLLAMA TAG)
参数量
量化格式
内存/显存需求
零幻觉遵循度
qwen2.5:7b-instruct-q4_K_M
7B
4-bit (Q4_K_M)
~5.5 GB
首选推荐：严格遵循 Prompt 负向指令（拒答率高），指令理解准确。
llama3.1:8b-instruct-q4_K_M
8B
4-bit (Q4_K_M)
~6.0 GB
备选推荐：英文与通用 CLI 逻辑优秀，中文技术问答表现良好。
qwen2.5:3b-instruct-q4_K_M
3B
4-bit (Q4_K_M)
~2.8 GB
极轻量模式：专用于 8GB 内存超低配机器，但复杂逻辑推理略弱。


2. 硬件配置基线与运行环境要求
系统支持纯 CPU 运算与 GPU/统一内存硬件加速两种运行模式：


+-------------------------------------------------------------------------+
|                              硬件配置基线                               |
+-------------------+-----------------------------+-----------------------+
| 硬件组件          | 最低要求 (纯 CPU 模式)      | 推荐配置 (加速模式)   |
+-------------------+-----------------------------+-----------------------+
| 处理器 (CPU)      | 4 核 Intel/AMD (需 AVX2)    | 8 核 x86 / M1/M2/M3   |
| 运行内存 (RAM)    | 16 GB DDR4                  | 16 GB / 32 GB 统一内存|
| 显卡 (GPU/VRAM)   | 无需独立显卡                | NVIDIA ≥ 8GB (RTX系) |
| 存储空间 (SSD)    | 20 GB NVMe/SATA             | 50 GB NVMe SSD        |
| 操作系统          | Ubuntu 20.04+ / Win 11 / macOS 13+ (Ventura 及以上) |
+-------------------+-----------------------------+-----------------------+
3. 容量预估与性能基线 (Capacity Sizing)
以企业级运维手册（PDF/Markdown）为例建立的容量规划模型：

3.1 存储与向量开销基准
文档规模示例：1,000 篇典型技术文档（平均每篇 10 页，共 10,000 页）。
切片总量预估：按 512 Tokens/Chunk 约生成 40,000 ~ 60,000 个向量切片。
向量库存储空间（基于 ChromaDB/SQLite 本地存储）：
向量索引（512 维 Float32）+ 元数据存储：约占用 600 MB ~ 1.2 GB 磁盘空间。
3.2 延迟性能基线 (Performance SLA)
知识库单次构建速度：
CPU 模式（8 线程）：约 150 ~ 250 页/分钟。
GPU/MPS 加速模式：约 800 ~ 1,200 页/分钟。
单次问答响应延迟（End-to-End Latency）：
向量检索耗时：< 100 ms（Top-K=4）。
LLM 首字生成（TTFT）：GPU: < 0.8s；纯 CPU: 1.5s ~ 3.0s。
完整回答耗时：标准技术指令生成耗时控制在 3 ~ 6s 内。